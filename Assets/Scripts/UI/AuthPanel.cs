using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class AuthPanel : MonoBehaviour
{
    [SerializeField] private TMP_Text _authorizationCodeText;
    [SerializeField] private TMP_Text _connectingEndpointNameText;

    [SerializeField] private Button _acceptConnectionButton;

    [SerializeField] private Button _rejectConnectionButton;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
